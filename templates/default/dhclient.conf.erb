# Generated by Chef 
# dhclient.conf simple configuration
#

option rfc3442-classless-static-routes code 121 = array of unsigned integer 8;
send host-name "<hostname>";
<%
def word_wrap(text, line_width = 40, shift=0) 
  return text if line_width <= 0
  text.gsub(/\n/, ' ').gsub(/(.{1,#{line_width}})(\s+|$)/, "#{' '*shift}\\1\n").strip
end
-%>

request <%= word_wrap node['dhcp-client']['request']['options'].join(', '), 60, 8 %>;
